package;

import buddy.*;
import buddy.Should;

import Git;

class TestMain extends BuddySuite implements Buddy {
    public function new()
    {
        describe('Git', function () {
            it('should return a command to commit to git', function () {

            });
        });

    }
}
